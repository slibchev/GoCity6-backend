name: gocity6_backend
description: A server app using the shelf package and Docker.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.13.1

dependencies:
  http: ^1.6.0
  shelf: ^1.4.2
  shelf_router: ^1.1.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
